# stm32开发板需求
## 硬件需求
 * stm32f103c8t6最小系统
 * 串口USART1预留(2.54mm排针)
 * 按键 x3
 * 0603 LED x2
 * ws2812 LED x3
 * m24c02 i2c flash

## 固件需求
1. 调通串口，电脑串口助手可以接收到单片机发送的串口数据
2. 调通printf，分别打印下面几种数据
    * 十进制数字（100）
    * 字符串（hello）
    * 带符号数字（-45）
    * 十六进制数字（0xAA）
    * 将数字177，同时选择用十进制和十六进制打印
    * 小数（3.14）
3. 调通串口接收，通过电脑串口助手发送数据给单片机
    * 在mdk keil5的调试模式里面打断点，单片机串口接收到数据之后，程序暂停运行
    * 同上，只是当串口接收的数据是字符's'的时候，程序暂停运行
    * 同上，只是当串口接收的数据是数字170，程序暂停运行
    * 单片机串口将接收的数据又转发回给电脑